{
 "AutoTaskRuleDao": {
  "fun all": "SELECT * FROM auto_task_rules ORDER BY customOrder, name COLLATE NOCASE, id",
  "fun flowAll": "SELECT * FROM auto_task_rules ORDER BY customOrder, name COLLATE NOCASE, id",
  "fun getById": "SELECT * FROM auto_task_rules WHERE id = :id",
  "fun maxOrder": "SELECT COALESCE(MAX(customOrder), -1) FROM auto_task_rules",
  "fun deleteByIds": "DELETE FROM auto_task_rules WHERE id IN (:ids)",
  "fun updateCron": "UPDATE auto_task_rules SET cron = :cron WHERE id IN (:ids)",
  "fun updateEnabled": "UPDATE auto_task_rules SET enable = :enabled WHERE id IN (:ids)",
  "fun clearRunLog": "UPDATE auto_task_rules SET lastResult = NULL, lastError = NULL, lastLog = NULL WHERE id = :id",
  "fun updateRunState": "UPDATE auto_task_rules SET lastRunAt = :lastRunAt, lastResult = :lastResult, lastError = :lastError, lastLog = :lastLog WHERE id = :id"
 },
 "BookChapterDao": {
  "fun search": "SELECT * FROM chapters where bookUrl = :bookUrl and `index` >= :start and `index` <= :end and title like '%'||:key||'%' order by `index`",
  "fun searchIndexes": "SELECT `index` FROM chapters where bookUrl = :bookUrl and `index` >= :start and `index` <= :end and title like '%'||:key||'%' order by `index`",
  "fun getChapterList": "select * from chapters where bookUrl = :bookUrl and `index` >= :start and `index` <= :end order by `index`",
  "fun getChapter": "select * from chapters where bookUrl = :bookUrl and `title` = :title",
  "fun getChapterCount": "select count(url) from chapters where bookUrl = :bookUrl",
  "fun updateContentMetadata": "update chapters set title = :title, imgUrl = :imgUrl where bookUrl = :bookUrl and `index` = :index",
  "fun delByBook": "delete from chapters where bookUrl = :bookUrl",
  "fun upWordCount": "update chapters set wordCount = :wordCount where bookUrl = :bookUrl and url = :url"
 },
 "BookDao": {
  "fun flowRoot": "select * from books where type & ${BookType.text} > 0 and type & ${BookType.local} = 0 and ((SELECT sum(groupId) FROM book_groups where groupId > 0) & `group`) = 0 and (select show from book_groups where groupId = ${BookGroup.IdNetNone}) != 1",
  "fun flowAll": "SELECT * FROM books order by durChapterTime desc",
  "fun flowAudio": "SELECT * FROM books WHERE type & ${BookType.audio} > 0",
  "fun flowVideo": "SELECT * FROM books WHERE type & ${BookType.video} > 0",
  "fun flowLocal": "SELECT * FROM books WHERE type & ${BookType.local} > 0",
  "fun flowNetNoGroup": "select * from books where type & ${BookType.audio} = 0 and type & ${BookType.local} = 0 and type & ${BookType.video} = 0 and ((SELECT sum(groupId) FROM book_groups where groupId > 0) & `group`) = 0",
  "fun flowLocalNoGroup": "select * from books where type & ${BookType.local} > 0 and ((SELECT sum(groupId) FROM book_groups where groupId > 0) & `group`) = 0",
  "fun flowByUserGroup": "SELECT * FROM books WHERE (`group` & :group) > 0",
  "fun flowSearch": "SELECT * FROM books WHERE name like '%'||:key||'%' or author like '%'||:key||'%'",
  "fun flowUpdateError": "SELECT * FROM books where type & ${BookType.updateError} > 0 order by durChapterTime desc",
  "fun getBooksByGroup": "SELECT * FROM books WHERE (`group` & :group) > 0",
  "fun findByName": "SELECT * FROM books WHERE `name` in (:names)",
  "fun getBookByFileName": "select * from books where originName = :fileName",
  "val localBookFileNames": "SELECT originName FROM books WHERE type & ${BookType.local} > 0",
  "val localBookAlternateOrigins": "SELECT origin FROM books WHERE type & ${BookType.local} > 0 \"AND origin != '${BookType.localTag}'",
  "fun getBook": "SELECT * FROM books WHERE name = :name and author = :author",
  "fun getAllUseBookSource": "select distinct bs.* from books, book_sources bs where origin == bookSourceUrl and origin not like '${BookType.localTag}%' and origin not like '${BookType.webDavTag}%'",
  "fun getBookByOrigin": "SELECT * FROM books WHERE name = :name and origin = :origin",
  "val noGroupSize": "select count(bookUrl) from books where (SELECT sum(groupId) FROM book_groups)",
  "val webBooks": "SELECT * FROM books where type & ${BookType.local} = 0",
  "val hasUpdateBooks": "SELECT * FROM books where type & ${BookType.local} = 0 and canUpdate = 1",
  "val all": "SELECT * FROM books",
  "fun getCacheCleanupBooks": "SELECT bookUrl, name, origin, originName, type FROM books",
  "fun getCacheCleanupImageBooks": "SELECT * FROM books WHERE (type & ${BookType.image}) > 0",
  "fun getByTypeOnLine": "SELECT * FROM books where type & :type > 0 and type & ${BookType.local} = 0",
  "val lastReadBook": "SELECT * FROM books where type & ${BookType.text} > 0 ORDER BY durChapterTime DESC limit 1",
  "val lastReadBookOnShelf": "SELECT * FROM books where type & ${BookType.notShelf} = 0 \"ORDER BY (durChapterIndex > 0 OR durChapterPos > 0) DESC, \"durChapterTime DESC limit 1",
  "val allBookUrls": "SELECT bookUrl FROM books",
  "fun findExistingBookUrls": "SELECT bookUrl FROM books WHERE bookUrl IN (:bookUrls)",
  "val allBookCount": "SELECT COUNT(*) FROM books",
  "fun flowShelfBookCount": "SELECT COUNT(*) FROM books where type & ${BookType.notShelf} = 0",
  "val readingCount": "SELECT count(*) FROM books where \"(durChapterIndex > 0 OR durChapterPos > 0) \"and type & ${BookType.notShelf} = 0",
  "val minOrder": "select min(`order`) from books",
  "val maxOrder": "select max(`order`) from books",
  "fun has": "select exists(select 1 from books where name = :name and author = :author)",
  "fun hasFile": "select exists(select 1 from books where type & ${BookType.local} > 0 and (originName = :fileName or (origin != '${BookType.localTag}' and origin like '%' || :fileName)))",
  "fun getReadConfigJson": "select readConfig from books where bookUrl = :bookUrl",
  "fun updateReadConfigJson": "update books set readConfig = :readConfig where bookUrl = :bookUrl",
  "fun upProgress": "update books set durChapterPos = :pos where bookUrl = :bookUrl",
  "fun updateShelfState": "update books set type = :type, `order` = :order where bookUrl = :bookUrl",
  "fun upGroup": "update books set `group` = :newGroupId where `group` = :oldGroupId",
  "fun removeGroup": "update books set `group` = `group` - :group where `group` & :group > 0",
  "fun deleteNotShelfBook": "delete from books where type & ${BookType.notShelf} > 0"
 },
 "BookGroupDao": {
  "fun getByID": "select * from book_groups where groupId = :id",
  "fun getByName": "select * from book_groups where groupName = :groupName",
  "fun flowAll": "SELECT * FROM book_groups ORDER BY `order`",
  "val show": "with const as (SELECT sum(groupId) sumGroupId FROM book_groups where groupId > 0) SELECT book_groups.* FROM book_groups join const where show > 0 and ( (groupId >= 0 and exists (select 1 from books where `group` & book_groups.groupId > 0)) or groupId = -1 or (groupId = -2 and exists (select 1 from books where type & ${BookType.local} > 0)) or (groupId = -3 and exists (select 1 from books where type & ${BookType.audio} > 0)) or (groupId = -6 and exists (select 1 from books where type & ${BookType.video} > 0)) or (groupId = -11 and exists (select 1 from books where type & ${BookType.updateError} > 0)) or (groupId = -4 and exists ( select 1 from books where type & ${BookType.audio} = 0 and type & ${BookType.video} = 0 and type & ${BookType.local} = 0 and const.sumGroupId & `group` = 0 ) ) or (groupId = -5 and exists ( select 1 from books where type & ${BookType.audio} = 0 and type & ${BookType.video} = 0 and type & ${BookType.local} > 0 and const.sumGroupId & `group` = 0 ) ) ) ORDER BY `order`",
  "fun flowSelect": "SELECT * FROM book_groups where groupId >= 0 ORDER BY `order`",
  "val idsSum": "SELECT sum(groupId) FROM book_groups where groupId >= 0",
  "val maxOrder": "SELECT MAX(`order`) FROM book_groups where groupId >= 0",
  "val all": "SELECT * FROM book_groups ORDER BY `order`",
  "val canAddGroup": "select count(*) < 63 from book_groups where groupId > 0",
  "fun enableGroup": "update book_groups set show = 1 where groupId = :groupId",
  "fun getGroupNames": "select groupName from book_groups where groupId > 0 and (groupId & :id) > 0"
 },
 "BookHighlightDao": {
  "val all": "select * from highlights order by bookName collate localized, bookAuthor collate localized, chapterIndex, chapterPos, time",
  "fun getByBook": "select * from highlights where bookUrl = :bookUrl order by chapterIndex, chapterPos, time",
  "fun flowByBook": "select * from highlights where bookUrl = :bookUrl order by chapterIndex, chapterPos, time",
  "fun flowSearch": "select * from highlights where bookUrl = :bookUrl and ( chapterName like '%' || :key || '%' or bookText like '%' || :key || '%' or note like '%' || :key || '%' ) order by chapterIndex, chapterPos, time",
  "fun pinLayoutTitleLength": "update highlights set layoutTitleLength = :layoutTitleLength where bookUrl = :bookUrl and chapterUrl = :chapterUrl and layoutTitleLength < 0",
  "fun bindChapterUrl": "update highlights set chapterUrl = :chapterUrl where time in (:times) and chapterUrl = ''",
  "fun updateBookMetadata": "update highlights set bookName = :bookName, bookAuthor = :bookAuthor where bookUrl = :bookUrl"
 },
 "BookSourceDao": {
  "fun flowAll": "select * from book_sources_part order by customOrder asc",
  "fun flowSearch": "select bp.* from book_sources b join book_sources_part bp on b.bookSourceUrl = bp.bookSourceUrl where b.bookSourceName like '%' || :searchKey || '%' or b.bookSourceGroup like '%' || :searchKey || '%' or b.bookSourceUrl like '%' || :searchKey || '%' or b.bookSourceComment like '%' || :searchKey || '%' order by b.customOrder asc",
  "fun search": "select * from book_sources where bookSourceName like '%' || :searchKey || '%' or bookSourceGroup like '%' || :searchKey || '%' or bookSourceUrl like '%' || :searchKey || '%' or bookSourceComment like '%' || :searchKey || '%' order by customOrder asc",
  "fun flowSearchEnabled": "select bp.* from book_sources b join book_sources_part bp on b.bookSourceUrl = bp.bookSourceUrl where b.enabled = 1 and (b.bookSourceName like '%' || :searchKey || '%' or b.bookSourceGroup like '%' || :searchKey || '%' or b.bookSourceUrl like '%' || :searchKey || '%' or b.bookSourceComment like '%' || :searchKey || '%') order by b.customOrder asc",
  "fun flowGroupSearch": "select t2.* from book_sources_part as t2 where \"\" NON_EMPTY_SOURCE_GROUP_CONDITION \"\" \"\" SOURCE_GROUP_MEMBERSHIP_FILTER \"\" order by t2.customOrder asc",
  "fun groupSearch": "select t2.* from book_sources as t2 where \"\" NON_EMPTY_SOURCE_GROUP_CONDITION \"\" \"\" SOURCE_GROUP_MEMBERSHIP_FILTER \"\" order by t2.customOrder asc",
  "fun flowEnabled": "select * from book_sources_part where enabled = 1 order by customOrder asc",
  "fun flowDisabled": "select * from book_sources_part where enabled = 0 order by customOrder asc",
  "fun flowExplore": "select * from book_sources_part where enabledExplore = 1 and hasExploreUrl = 1 and (bookSourceGroup like '%' || :key || '%' or bookSourceName like '%' || :key || '%') order by customOrder asc",
  "fun flowLogin": "select * from book_sources_part where hasLoginUrl = 1 order by customOrder asc",
  "fun flowNoGroup": "select * from book_sources_part where \"\" BOOK_SOURCE_NO_GROUP_FILTER \"\" order by customOrder asc",
  "fun flowEnabledExplore": "select * from book_sources_part where enabledExplore = 1 order by customOrder asc",
  "fun flowDisabledExplore": "select * from book_sources_part where enabledExplore = 0 order by customOrder asc",
  "fun flowGroupExplore": "select t2.* from book_sources_part as t2 where t2.enabledExplore = 1 and t2.hasExploreUrl = 1 and \"\" NON_EMPTY_SOURCE_GROUP_CONDITION \"\" \"\" SOURCE_GROUP_MEMBERSHIP_FILTER \"\" order by t2.customOrder asc",
  "fun flowGroupsUnProcessed": "select distinct bookSourceGroup from book_sources where trim(bookSourceGroup) <> ''",
  "fun flowEnabledGroupsUnProcessed": "select distinct bookSourceGroup from book_sources where enabled = 1 and trim(bookSourceGroup) <> ''",
  "fun flowExploreGroupsUnProcessed": "select distinct bookSourceGroup from book_sources where enabledExplore = 1 and trim(exploreUrl) <> '' and trim(bookSourceGroup) <> '' order by customOrder",
  "fun getByGroup": "select * from book_sources where bookSourceGroup like '%' || :group || '%' order by customOrder asc",
  "fun getEnabledByGroup": "select * from book_sources where enabled = 1 and (bookSourceGroup = :group or bookSourceGroup like :group || ',%' or bookSourceGroup like '%,' || :group or bookSourceGroup like '%,' || :group || ',%') order by customOrder asc",
  "fun getEnabledPartByGroup": "select t2.* from book_sources_part as t2 where t2.enabled = 1 \"\" SOURCE_GROUP_MEMBERSHIP_FILTER \"\" order by t2.customOrder asc",
  "fun getEnabledByType": "select * from book_sources where bookUrlPattern != 'NONE' and bookSourceType = :type order by customOrder asc",
  "fun getBookSourceAddBook": "select * from book_sources where enabled = 1 and bookSourceUrl = :baseUrl",
  "val hasBookUrlPattern": "select bp.* from book_sources b join book_sources_part bp on b.bookSourceUrl = bp.bookSourceUrl where b.enabled = 1 and trim(b.bookUrlPattern) <> '' and trim(b.bookUrlPattern) <> 'NONE' order by b.customOrder",
  "val noGroup": "select * from book_sources where bookSourceGroup is null or bookSourceGroup = ''",
  "val all": "select * from book_sources order by customOrder asc",
  "val allPart": "select * from book_sources_part order by customOrder asc",
  "val allEnabled": "select * from book_sources where enabled = 1 order by customOrder",
  "val allEnabledPart": "select * from book_sources_part where enabled = 1 order by customOrder asc",
  "val allDisabled": "select * from book_sources where enabled = 0 order by customOrder",
  "val allNoGroup": "select * from book_sources where",
  "val allEnabledExplore": "select * from book_sources where enabledExplore = 1 order by customOrder",
  "val allDisabledExplore": "select * from book_sources where enabledExplore = 0 order by customOrder",
  "val allLogin": "select * from book_sources where loginUrl is not null and loginUrl != ''",
  "val allTextEnabledPart": "select bp.* from book_sources b join book_sources_part bp on b.bookSourceUrl = bp.bookSourceUrl where b.enabled = 1 and b.bookSourceType = 0 order by b.customOrder",
  "val allGroupsUnProcessed": "select distinct bookSourceGroup from book_sources where trim(bookSourceGroup) <> ''",
  "val allEnabledGroupsUnProcessed": "select distinct bookSourceGroup from book_sources where enabled = 1 and trim(bookSourceGroup) <> ''",
  "fun getBookSource": "select * from book_sources where bookSourceUrl = :key",
  "fun getBookSources": "select * from book_sources where bookSourceUrl in (:keys)",
  "fun getBookSourcePart": "select * from book_sources_part where bookSourceUrl = :key",
  "fun allCount": "select count(*) from book_sources",
  "fun has": "SELECT EXISTS(select 1 from book_sources where bookSourceUrl = :key)",
  "fun getMainJs": "select mainJs from book_sources where bookSourceUrl = :key",
  "fun updateCheckResult": "update book_sources set bookSourceGroup = :bookSourceGroup, bookSourceComment = :bookSourceComment, respondTime = :respondTime where bookSourceUrl = :bookSourceUrl and lastUpdateTime = :expectedLastUpdateTime and ((bookSourceGroup is null and :expectedBookSourceGroup is null) or bookSourceGroup = :expectedBookSourceGroup) and ((bookSourceComment is null and :expectedBookSourceComment is null) or bookSourceComment = :expectedBookSourceComment) and respondTime = :expectedRespondTime",
  "fun delete": "delete from book_sources where bookSourceUrl = :key",
  "val minOrder": "select min(customOrder) from book_sources",
  "val maxOrder": "select max(customOrder) from book_sources",
  "val hasDuplicateOrder": "select exists (select 1 from book_sources group by customOrder having count(customOrder) > 1)",
  "fun enable": "update book_sources set enabled = :enable where bookSourceUrl = :bookSourceUrl",
  "fun enableExplore": "update book_sources set enabledExplore = :enable where bookSourceUrl = :bookSourceUrl",
  "fun upOrder": "update book_sources set customOrder = :customOrder where bookSourceUrl = :bookSourceUrl",
  "fun upGroup": "update book_sources set bookSourceGroup = :bookSourceGroup where bookSourceUrl = :bookSourceUrl"
 },
 "BookmarkDao": {
  "val all": "select * from bookmarks order by bookName collate localized, bookAuthor collate localized, chapterIndex, chapterPos",
  "fun flowAll": "select * from bookmarks order by bookName collate localized, bookAuthor collate localized, chapterIndex, chapterPos",
  "fun flowByBook": "select * from bookmarks where bookName = :bookName and bookAuthor = :bookAuthor order by chapterIndex",
  "fun flowSearch": "SELECT * FROM bookmarks where bookName = :bookName and bookAuthor = :bookAuthor and ( chapterName like '%'||:key||'%' or content like '%'||:key||'%' ) order by chapterIndex",
  "fun getByBook": "select * from bookmarks where bookName = :bookName and bookAuthor = :bookAuthor order by chapterIndex",
  "fun search": "SELECT * FROM bookmarks where bookName = :bookName and bookAuthor = :bookAuthor and ( chapterName like '%'||:key||'%' or content like '%'||:key||'%' ) order by chapterIndex"
 },
 "CacheDao": {
  "fun get": "select value from caches where `key` = :key and (deadline = 0 or deadline > :now)",
  "fun delete": "delete from caches where `key` = :key",
  "fun deleteSourceVariables": "delete from caches where `key` like 'v_' || :key || '_%' or `key` = 'userInfo_' || :key or `key` = 'loginHeader_' || :key or `key` = 'sourceVariable_' || :key or `key` = 'infoMap_' || :key",
  "fun clearDeadline": "delete from caches where deadline > 0 and deadline < :now"
 },
 "CookieDao": {
  "fun get": "SELECT * FROM cookies Where url = :url",
  "fun getOkHttpCookies": "select * from cookies where url like '%|%'",
  "fun delete": "delete from cookies where url = :url",
  "fun deleteOkHttp": "delete from cookies where url like '%|%'"
 },
 "DictRuleDao": {
  "val all": "select * from dictRules order by sortNumber",
  "val enabled": "select * from dictRules where enabled = 1 order by sortNumber",
  "fun flowAll": "select * from dictRules order by sortNumber",
  "fun getByName": "select * from dictRules where name = :name"
 },
 "HighlightRuleDao": {
  "val all": "SELECT * FROM highlightRules ORDER BY sortOrder ASC",
  "fun flowAll": "SELECT * FROM highlightRules ORDER BY sortOrder ASC",
  "fun findById": "SELECT * FROM highlightRules WHERE id = :id",
  "fun findEnabledByBook": "SELECT * FROM highlightRules WHERE isEnabled = 1 AND (scope IS NULL OR scope = '' OR (:name != '' AND instr(scope, :name) > 0) OR (:origin != '' AND instr(scope, :origin) > 0)) ORDER BY sortOrder ASC",
  "val minOrder": "SELECT ifnull(min(sortOrder), 0) FROM highlightRules",
  "val maxOrder": "SELECT ifnull(max(sortOrder), 0) FROM highlightRules",
  "fun deleteAll": "DELETE FROM highlightRules"
 },
 "HttpTTSDao": {
  "val all": "select * from httpTTS order by name",
  "fun flowAll": "select * from httpTTS order by name",
  "val count": "select count(*) from httpTTS",
  "fun get": "select * from httpTTS where id = :id",
  "fun getName": "select name from httpTTS where id = :id",
  "fun deleteDefault": "delete from httpTTS where id < 0"
 },
 "KeyboardAssistsDao": {
  "val all": "select * from keyboardAssists order by serialNo",
  "fun getByType": "select * from keyboardAssists where type = :type order by serialNo",
  "val flowAll": "select * from keyboardAssists order by serialNo",
  "fun flowByType": "select * from keyboardAssists where type = :type order by serialNo",
  "val maxSerialNo": "select max(serialNo) from keyboardAssists order by serialNo"
 },
 "ReadRecordDao": {
  "val all": "select * from readRecord",
  "fun flowBooks": "select distinct bookName, author from readRecord order by bookName collate localized, author collate localized",
  "val allShow": "select bookName, sum(readTime) as readTime, max(lastRead) as lastRead from readRecord group by bookName order by bookName collate localized",
  "val allTime": "select sum(readTime) from readRecord",
  "fun search": "select bookName, sum(readTime) as readTime, max(lastRead) as lastRead from readRecord where bookName like '%' || :searchKey || '%' group by bookName order by bookName collate localized",
  "fun getReadTime": "select sum(readTime) from readRecord where bookName = :bookName",
  "fun getRecord": "select * from readRecord where deviceId = :deviceId and bookName = :bookName",
  "fun getAuthor": "select author from readRecord where deviceId = :deviceId and bookName = :bookName",
  "fun clear": "delete from readRecord",
  "fun deleteByName": "delete from readRecord where bookName = :bookName"
 },
 "ReplaceRuleDao": {
  "fun flowAll": "SELECT * FROM replace_rules ORDER BY sortOrder ASC",
  "fun flowSearch": "SELECT * FROM replace_rules where `group` like :key or name like :key ORDER BY sortOrder ASC",
  "fun flowEnabled": "SELECT * FROM replace_rules WHERE isEnabled = 1 ORDER BY sortOrder ASC",
  "fun flowDisabled": "SELECT * FROM replace_rules WHERE isEnabled = 0 ORDER BY sortOrder ASC",
  "fun flowGroupSearch": "SELECT t2.* FROM replace_rules AS t2 WHERE \"\" REPLACE_RULE_GROUP_FILTER \"\" ORDER BY t2.sortOrder ASC",
  "fun flowGroupsUnProcessed": "select `group` from replace_rules where `group` is not null and `group` <> ''",
  "fun flowNoGroup": "select * from replace_rules where",
  "val minOrder": "SELECT MIN(sortOrder) FROM replace_rules",
  "val maxOrder": "SELECT MAX(sortOrder) FROM replace_rules",
  "val all": "SELECT * FROM replace_rules ORDER BY sortOrder ASC",
  "val allGroupsUnProcessed": "select distinct `group` from replace_rules where trim(`group`) <> ''",
  "val allEnabled": "SELECT * FROM replace_rules WHERE isEnabled = 1 ORDER BY sortOrder ASC",
  "fun findById": "SELECT * FROM replace_rules WHERE id = :id",
  "fun findByIds": "SELECT * FROM replace_rules WHERE id in (:ids)",
  "fun findEnabledByContentScope": "SELECT * FROM replace_rules WHERE isEnabled = 1 and scopeContent = 1 AND (scope LIKE '%' || :name || '%' or scope LIKE '%' || :origin || '%' or scope is null or scope = '') and (excludeScope is null or (excludeScope not LIKE '%' || :name || '%' and excludeScope not LIKE '%' || :origin || '%')) order by sortOrder",
  "fun findEnabledByTitleScope": "SELECT * FROM replace_rules WHERE isEnabled = 1 and scopeTitle = 1 AND (scope LIKE '%' || :name || '%' or scope LIKE '%' || :origin || '%' or scope is null or scope = '') and (excludeScope is null or (excludeScope not LIKE '%' || :name || '%' and excludeScope not LIKE '%' || :origin || '%')) order by sortOrder",
  "fun getByGroup": "select * from replace_rules where `group` like '%' || :group || '%'",
  "val noGroup": "select * from replace_rules where `group` is null or `group` = ''",
  "val summary": "SELECT COUNT(*) - SUM(isEnabled) FROM replace_rules",
  "fun enableAll": "UPDATE replace_rules SET isEnabled = :enable"
 },
 "RssArticleDao": {
  "fun get": "select * from rssArticles where origin = :origin and link = :link and sort = :sort",
  "fun getByLink": "select * from rssArticles where origin = :origin and link = :link",
  "fun flowByOriginSort": "select t1.link, t1.sort, t1.origin, t1.`order`, t1.title, t1.content, t1.description, t1.image, t1.`group`, t1.pubDate, t1.variable, t1.type, t1.durPos, ifNull(t2.read, 0) as read from rssArticles as t1 left join rssReadRecords as t2 on t1.link = t2.record where t1.origin = :origin and t1.sort = :sort order by `order` desc",
  "fun clearOld": "delete from rssArticles where origin = :origin and sort = :sort and `order` < :order",
  "fun updateOrigin": "update rssArticles set origin = :origin where origin = :oldOrigin",
  "fun delete": "delete from rssArticles where origin = :origin"
 },
 "RssReadRecordDao": {
  "fun getRecords": "select * from rssReadRecords order by readTime desc",
  "fun getRecordsByOrigin": "select * from rssReadRecords where origin = :origin order by readTime desc",
  "fun getRecord": "select * from rssReadRecords where record = :record and origin = :origin",
  "val countRecords": "select count(1) from rssReadRecords",
  "fun countRecordsByOrigin": "select count(1) from rssReadRecords where origin = :origin",
  "fun deleteAllRecord": "delete from rssReadRecords",
  "fun deleteRecordsByOrigin": "delete from rssReadRecords where origin = :origin"
 },
 "RssSourceDao": {
  "fun getByKey": "select * from rssSources where sourceUrl = :key",
  "fun getRssSources": "select * from rssSources where sourceUrl in (:sourceUrls)",
  "fun findExistingSourceUrls": "select sourceUrl from rssSources where sourceUrl in (:sourceUrls)",
  "val all": "SELECT * FROM rssSources order by customOrder",
  "val size": "select count(sourceUrl) from rssSources",
  "fun flowAll": "SELECT * FROM rssSources order by customOrder",
  "fun flowSearch": "SELECT * FROM rssSources where sourceName like '%' || :key || '%' or sourceUrl like '%' || :key || '%' or sourceGroup like '%' || :key || '%' or sourceComment like '%' || :key || '%' order by customOrder",
  "fun flowGroupSearch": "SELECT t2.* FROM rssSources AS t2 where \"\" RSS_SOURCE_GROUP_FILTER \"\" order by t2.customOrder",
  "fun flowEnabled": "SELECT * FROM rssSources where enabled = 1 and (sourceName like '%' || :searchKey || '%' or sourceGroup like '%' || :searchKey || '%' or sourceUrl like '%' || :searchKey || '%' or sourceComment like '%' || :searchKey || '%') order by customOrder",
  "fun flowDisabled": "SELECT * FROM rssSources where enabled = 0 order by customOrder",
  "fun flowLogin": "select * from rssSources where loginUrl is not null and loginUrl != ''",
  "fun flowNoGroup": "select * from rssSources where",
  "fun flowEnabledByGroup": "SELECT t2.* FROM rssSources AS t2 where t2.enabled = 1 and \"\" RSS_SOURCE_GROUP_FILTER \"\" order by t2.customOrder",
  "fun flowGroupsUnProcessed": "select distinct sourceGroup from rssSources where trim(sourceGroup) <> ''",
  "fun flowEnabledGroupsUnProcessed": "select distinct sourceGroup from rssSources where trim(sourceGroup) <> '' and enabled = 1",
  "val allGroupsUnProcessed": "select distinct sourceGroup from rssSources where trim(sourceGroup) <> ''",
  "val minOrder": "select min(customOrder) from rssSources",
  "val maxOrder": "select max(customOrder) from rssSources",
  "fun delete": "delete from rssSources where sourceUrl = :sourceUrl",
  "fun deleteDefault": "delete from rssSources where sourceGroup like 'legado'",
  "val noGroup": "select * from rssSources where sourceGroup is null or sourceGroup = ''",
  "fun getByGroup": "select * from rssSources where sourceGroup like '%' || :group || '%'",
  "fun has": "select exists(select 1 from rssSources where sourceUrl = :key)",
  "fun enable": "update rssSources set enabled = :enable where sourceUrl = :sourceUrl"
 },
 "RssStarDao": {
  "val all": "select * from rssStars order by starTime desc",
  "fun flowGroups": "select `group` from rssStars group by `group` order by `group`",
  "fun flowByGroup": "select * from rssStars where `group` = :group order by starTime desc",
  "fun get": "select * from rssStars where origin = :origin and link = :link",
  "fun liveAll": "select * from rssStars order by starTime desc",
  "fun updateOrigin": "update rssStars set origin = :origin where origin = :oldOrigin",
  "fun delete": "delete from rssStars where origin = :origin and link = :link",
  "fun deleteByGroup": "delete from rssStars where `group` = :group",
  "fun deleteAll": "delete from rssStars"
 },
 "RuleSubDao": {
  "val all": "select * from ruleSubs order by customOrder",
  "fun flowAll": "select * from ruleSubs order by customOrder",
  "val maxOrder": "select customOrder from ruleSubs order by customOrder limit 0,1",
  "fun findByUrl": "select * from ruleSubs where url = :url"
 },
 "SearchBookDao": {
  "fun getSearchBook": "select * from searchBooks where bookUrl = :bookUrl",
  "fun getFirstByNameAuthor": "select * from searchBooks where name = :name and author = :author and origin in (select bookSourceUrl from book_sources) order by originOrder limit 1",
  "fun changeSourceByGroup": "select t1.name, t1.author, t1.origin, t1.originName, t1.coverUrl, t1.bookUrl, t1.type, t1.time, t1.intro, t1.kind, t1.latestChapterTitle, t1.tocUrl, t1.variable, t1.wordCount, t2.customOrder as originOrder, t1.chapterWordCountText, t1.respondTime, t1.chapterWordCount from searchBooks as t1 inner join book_sources as t2 on t1.origin = t2.bookSourceUrl where t1.name = :name and t1.author like '%'||:author||'%' and t2.enabled = 1 \"\" SOURCE_GROUP_MEMBERSHIP_FILTER \"\" order by t2.customOrder",
  "fun changeSourceSearch": "select t1.name, t1.author, t1.origin, t1.originName, t1.coverUrl, t1.bookUrl, t1.type, t1.time, t1.intro, t1.kind, t1.latestChapterTitle, t1.tocUrl, t1.variable, t1.wordCount, t2.customOrder as originOrder, t1.chapterWordCountText, t1.respondTime, t1.chapterWordCount from searchBooks as t1 inner join book_sources as t2 on t1.origin = t2.bookSourceUrl where t1.name = :name and t1.author like '%'||:author||'%' \"\" SOURCE_GROUP_MEMBERSHIP_FILTER \"\" and (originName like '%'||:key||'%' or t1.latestChapterTitle like '%'||:key||'%') and t2.enabled = 1 order by t2.customOrder",
  "fun getEnableHasCover": "select t1.name, t1.author, t1.origin, t1.originName, t1.coverUrl, t1.bookUrl, t1.type, t1.time, t1.intro, t1.kind, t1.latestChapterTitle, t1.tocUrl, t1.variable, t1.wordCount, t2.customOrder as originOrder, t1.chapterWordCountText, t1.respondTime, t1.chapterWordCount from searchBooks as t1 inner join book_sources as t2 on t1.origin = t2.bookSourceUrl where t1.name = :name and t1.author = :author and t1.coverUrl is not null and t1.coverUrl <> '' and t2.enabled = 1 order by t2.customOrder",
  "fun clear": "delete from searchBooks where name = :name and author = :author",
  "fun clearExpired": "delete from searchBooks where time < :time"
 },
 "SearchKeywordDao": {
  "val all": "SELECT * FROM search_keywords",
  "fun flowByUsage": "SELECT * FROM search_keywords ORDER BY usage DESC",
  "fun flowByTime": "SELECT * FROM search_keywords ORDER BY lastUseTime DESC",
  "fun flowSearch": "SELECT * FROM search_keywords where word like '%'||:key||'%' ORDER BY usage DESC",
  "fun get": "select * from search_keywords where word = :key",
  "fun deleteAll": "DELETE FROM search_keywords"
 },
 "ServerDao": {
  "fun observeAll": "select * from servers order by sortNumber",
  "val all": "select * from servers order by sortNumber",
  "fun get": "select * from servers where id = :id",
  "fun delete": "delete from servers where id = :id",
  "fun deleteDefault": "delete from servers where id < 0"
 },
 "TxtTocRuleDao": {
  "fun observeAll": "select * from txtTocRules order by serialNumber",
  "val all": "select * from txtTocRules order by serialNumber",
  "val enabled": "select * from txtTocRules where enable = 1 order by serialNumber",
  "val disabled": "select * from txtTocRules where enable != 1 order by serialNumber",
  "val count": "select count(*) from txtTocRules",
  "fun get": "select * from txtTocRules where id = :id",
  "val minOrder": "select ifNull(min(serialNumber), 0) from txtTocRules",
  "val maxOrder": "select ifNull(max(serialNumber), 0) from txtTocRules",
  "fun deleteDefault": "delete from txtTocRules where id < 0"
 }
}